# Achievements

Placeholder for the future Achievements module (streaks, badges, completion stats). Mirrors the structure of `features/tasks`:

```
features/achievements/
├── components/
├── hooks/
└── lib/
```

Not part of the MVP — scaffolded so the module can be built without restructuring.
