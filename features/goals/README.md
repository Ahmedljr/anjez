# Goals

Placeholder for the future Goals module. Mirrors the structure of `features/tasks`:

```
features/goals/
├── components/
├── hooks/
└── lib/
```

Not part of the MVP — scaffolded so the module can be built without restructuring.
